# ============================================================
# requirements_v2.txt — Top-Level Only (Clean Version)
# Post Call Quality Analysis
# Only direct/first-party imports are listed here.
# Transitive deps are resolved automatically by pip.
# ============================================================

# ── Web Framework ──────────────────────────────────────────
Flask==3.1.1

# ── Microsoft Authentication ───────────────────────────────
msal==1.32.3

# ── Database ───────────────────────────────────────────────
pymongo==4.13.2
dnspython==2.7.0
SQLAlchemy==2.0.36
PyMySQL==1.1.1
pyodbc==5.1.0

# ── Task Scheduling ────────────────────────────────────────
APScheduler==3.11.0

# ── HTTP Requests ──────────────────────────────────────────
requests==2.32.4
certifi==2025.1.31

# ── Audio Processing ───────────────────────────────────────
pydub==0.25.1
librosa==0.11.0

# ── Azure Cloud Storage ────────────────────────────────────
azure-storage-blob==12.19.0

# ── Google APIs (Cloud Sync) ───────────────────────────────
google-api-python-client==2.172.0
google-auth==2.40.3
google-auth-oauthlib==1.2.2
google-auth-httplib2==0.2.0

# ── AI / ML — TensorFlow ───────────────────────────────────
# Windows CPU: replace 'tensorflow' with 'tensorflow-intel'
tensorflow==2.15.0
keras==2.15.0

# ── AI / ML — PyTorch ──────────────────────────────────────
# torch 2.7.1 does NOT exist on PyPI. Use 2.2.2 (stable).
# For CUDA builds visit: https://pytorch.org/get-started/locally/
torch==2.2.2

# ── AI / ML — HuggingFace ──────────────────────────────────
transformers==4.39.3
tokenizers==0.15.2
safetensors==0.4.5

# ── Numerical & Scientific ─────────────────────────────────
# numpy < 2.0 is required by tensorflow==2.15.0
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.4.2

# ── Environment & Config ───────────────────────────────────
python-dotenv==1.1.1
